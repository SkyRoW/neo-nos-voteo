import { LocalizationDatePipePipe } from './localization-date-pipe.pipe';

describe('LocalizationDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizationDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
